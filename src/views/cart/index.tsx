import React from 'react'
import { Button } from 'antd-mobile'

const Cart: React.FC = React.memo(function Cart() {
  return (
    <div>
      <Button>123</Button>
    </div>
  )
})

export default Cart
